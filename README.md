# Build instructions

- Build the android app from the ```mobileFrontend/``` folder (provide build instructions)
- Use Wifi Debugging with your cellphone instead of Emulator.
- Build Phoenix project:
```bash
mix phx.server # start server
```
```bash
ngrok http 4000 # to expose the phoenix server with a gateway
```

